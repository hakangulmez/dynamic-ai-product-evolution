# Source Package

Implementation modules should mirror the architecture boundaries: registry, discovery, ingestion, parsing, extraction, matching, measurement, evaluation, analysis, and provenance. Do not collapse stages into a monolithic pipeline.
