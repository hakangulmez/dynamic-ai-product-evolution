# Change Requests

Every behavior-changing prompt edit must have a change request in this directory.

Use the naming convention:

```text
CR-XXXX-short-title.md
```

Start from `evals/templates/change_request.template.md`.
