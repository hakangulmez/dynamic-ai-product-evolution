# Tests

Test categories:

- `unit`: deterministic functions.
- `schema`: JSON-schema validity and examples.
- `temporal`: future-leakage and cutoff rules.
- `provenance`: hashes and passage resolution.
- `contamination`: clean-room enforcement.
- `integration`: sentinel pipeline stages.
