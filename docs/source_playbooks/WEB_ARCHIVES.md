# Web Archives Playbook


## Scope

Historical snapshots of official pages when current pages are not temporally valid.

## Requirements

Store original URL, archive URL, snapshot time, retrieval time, response status, and content hash. Reject snapshots captured after the observation date unless the page itself contains a reliable earlier publication date and the temporal policy allows it.
